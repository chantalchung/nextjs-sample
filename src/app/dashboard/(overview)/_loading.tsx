import DashboardSkeleton from '@/src/app/ui/skeletons';

// not in use - should be just named loading.tsx to stream the whole page
export default function Loading() {
  return <DashboardSkeleton />;
}
